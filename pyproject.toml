[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "mazegen"
author = [
  { name="finorako", email="finorako@student.42antananrivo.mg" }
]
version = "0.1.0"
description = "Maze generation package"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["maze"]
