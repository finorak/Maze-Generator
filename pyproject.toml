[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "maze_gen"
version = "0.1.0"
dependencies = [
    "requests",
    "numpy"
]

[tool.setuptools.packages.find]
where = ["."]
include = ["maze_gen*", "src*"]